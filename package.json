{
  "name": "mern-graphql-full-stack",
  "version": "1.0.0",
  "scripts": {
    "postinstall": "npm install --prefix server && npm install --prefix client"
  },
  "engines": {
    "node": "8.11.1",
    "npm": "6.8.0"
  }
}
